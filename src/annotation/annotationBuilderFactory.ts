import { BaseAnnotation } from "./annotation";

/**
 * 注解建造者工厂
 */
export class AnnotationBuilderFactory {

    public static getAnnotationBuilder(classOrObjectName: string | BaseAnnotation) {
        // 若传入的是对象则
    }
}